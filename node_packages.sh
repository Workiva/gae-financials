#!/bin/bash

#Go to http://nodejs.org/ and install node.js and npm (which comes with node)

npm install -g coffee-script
npm install -g mocha
npm install -g less
npm install uglify-js
npm install coffeelint
